#include <stdio.h>
int main() {
    int i, n, count = 0;
    printf("Enter n: ");
    scanf("%d", &n);
    for(i = 1; count < n; i++) {
        if(i % 2 != 0) {
            printf("%d ", i);
            count++;
        }
    }
    return 0;
}
